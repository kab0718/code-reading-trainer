# MVP実装計画

## 1. この文書の目的

Code Reading TrainerのMVPを、動作確認可能な小さな単位に分けて実装するための作業手順を定める。

最初からすべての機能を並行して作るのではなく、まず現在のPythonファイルからおすすめ候補を選ぶ一連の体験を完成させる。次に、選んだ候補を使うトレーニングと読解サポートを接続する。

## 2. MVPの前提

- 対象はGitHub上のpublic repository
- 対応言語はPython
- Chrome Manifest V3のサイドパネル拡張として提供する
- ユーザーはトレーニングまたは読解サポートを選択する
- トレーニングではPythonコードを読み、説明を1回だけ回答する
- 回答後に100点満点の総合点、観点別得点、良かった点、不足点、模範解答を表示する
- 回答前には模範解答を表示しない
- 回答の書き直しと再評価は初期版に含めない
- 読解サポートでは、質問または調査目的に対して、最初に着眼点、確認質問、段階的なヒントを表示する
- 詳しい説明は、ユーザーが明示的に求めた場合だけ表示する
- 複数ファイルを横断する解析やコード実行は行わない

## 3. 推奨する実装順序

```text
仕様確定
  ↓
おすすめ候補の抽出と選択UI
  ↓
評価APIとの接続
  ↓
採点結果・模範解答の表示
  ↓
読解サポートの追加
  ↓
エラー処理・計測・リリース確認
```

おすすめ候補の選択に入口を一本化し、「候補を選ぶ、回答する、採点を見る」というトレーニングの中心体験を確立する。同じ対象コードを読解サポートにも渡し、実際のOSS読解を助ける用途を検証する。

## 4. フェーズ0: 実装前の仕様確定

評価軸の配点、再配分、評価APIの入出力・入力上限・エラー・タイムアウトは、[評価仕様・API契約 v1](evaluation-api-contract.md) で確定済みとする。クライアントとAPIは `contracts/evaluation/v1/` のJSON Schemaを共通利用する。

### 作業

- 評価APIの配置先と実装方式を決める
- APIの利用者認証と利用回数制限を決める
- コード、回答、採点結果を保存するか決める
- AIへコードと回答を送信することの表示方法を決める
- 読解サポートでAIへコードと質問を送信することの表示方法を決める
- Pythonファイルであることの判定方法を決める

### 推奨する初期方針

- 履歴は保存せず、現在のサイドパネル内だけで保持する
- Python判定はGitHub上のファイルパスが `.py` で終わることを基本とする
- トレーニングでは候補コードをそのまま評価対象にする
- 読解サポートでも候補コードを対象とし、リポジトリ全体を解析したような説明は行わない
- AIの認証情報は拡張機能に埋め込まず、バックエンドの評価APIで管理する
- APIレスポンスは自由文ではなく、固定されたJSON形式にする

### 完了条件

- 未決事項が選択肢ではなく、実装時に参照できる決定として記録されている
- APIのリクエストとレスポンス形式が確定している
- 採点結果画面に必要なデータがすべてAPIレスポンスに含まれている
- 読解サポートの支援段階と、各段階で表示する内容が確定している

## 5. フェーズ1: トレーニングの入力体験

### 作業

1. GitHubのコード画面かどうかを判定する
2. 開いているファイルがPythonか判定する
3. ページURL、ファイル情報、commit OIDをContent Scriptから取得する
4. 現在のファイルから抽出したおすすめ候補を表示する
5. ユーザーが選んだ候補コードをサイドパネルに表示する
6. ユーザーが説明を入力できるようにする
7. 候補なし、Python以外、空回答、長すぎる入力を検証する
8. 回答送信中は二重送信できないようにする

### 画面状態

- GitHub以外を開いている
- Pythonファイル以外を開いている
- おすすめ候補が見つからない
- 回答入力中
- 採点中
- 採点完了
- エラー発生

### 完了条件

- 選んだ候補のPythonコードが欠けずにサイドパネルへ表示される
- 候補がない場合と空回答時に、次に行う操作が分かるメッセージが表示される
- 採点ボタンを押すと、コードと回答を評価処理へ渡せる
- HTMLとして解釈されることなく、コードと回答が安全に表示される

## 6. フェーズ2: 評価APIと採点ロジック

### 作業

1. 評価APIのエンドポイントを作成する
2. リクエストの形式とサイズを検証する
3. Pythonコードとユーザー回答をAIモデルへ渡すプロンプトを作成する
4. 対象コードに適用する評価軸をAIに判定させる
5. 観点別得点と総合点の整合性を検証する
6. AI出力を固定JSONへ変換し、不正な出力を拒否または再生成する
7. タイムアウト、回数制限、モデルエラーを処理する
8. URLや回答内容など、不要な情報をログへ残さないようにする
9. CloudflareアカウントとWorkers AIの無料枠を確認する
10. AI binding、Rate Limiting binding、許可する拡張機能ID、本番Origin制限を設定する
11. dry-run bundleを確認してWorkerをデプロイし、公開URLを拡張機能へ設定する
12. 本番URLで正常系、CORS、入力エラー、回数制限を疎通確認する
13. Neuron使用量とAPIエラーを監視し、ロールバック手順を確認する

Cloudflare固有の設定値、コマンド、デプロイ後の確認項目は[評価APIの構成と運用](evaluation-api-operations.md)を作業手順の正本とする。MVPではWorkers AIの無料割当を上限とし、有料プランや有料モデルへ自動移行しない。

### 評価APIのデータ契約

実装時は [評価仕様・API契約 v1](evaluation-api-contract.md) と `contracts/evaluation/v1/` を正本とする。以下は主要フィールドの概要である。

リクエスト:

```json
{
  "language": "python",
  "sourceUrl": "https://github.com/owner/repository/blob/main/example.py",
  "code": "def example(...):\n    ...",
  "explanation": "この関数は..."
}
```

レスポンス:

```json
{
  "requestId": "4fd0d833-6bad-4d6e-b2e2-7fd9ba73710b",
  "contractVersion": "1.0",
  "totalScore": 78,
  "criteria": [
    {
      "id": "purpose",
      "label": "目的・責務",
      "applicable": true,
      "baseWeight": 25,
      "score": 20,
      "maxScore": 25,
      "feedback": "処理の目的を説明できています。",
      "exclusionReason": null
    }
  ],
  "strengths": ["入力を検証している点を説明できています。"],
  "gaps": ["例外が発生する条件の説明がありません。"],
  "modelAnswer": "この関数は...",
  "evaluatedAt": "2026-08-15T12:00:00Z"
}
```

`criteria` は概要を示すため1件だけ記載している。実際のレスポンスは6軸を固定順で返す。

### API側で保証すること

- `totalScore` は0以上100以下の整数
- `score` は0以上 `maxScore` 以下
- 採点対象となる `maxScore` の合計は100
- `criteria` には6軸を固定順で含め、対象外軸は `applicable: false` として明示する
- `modelAnswer` はコードから判断できない事実を断定しない
- 模範解答は採点処理が完了するまでクライアントへ返さない

### 完了条件

- 複数の難易度のPythonコードに対して、形式の崩れていない採点結果が返る
- 同じコードと同程度の回答に対する点数が極端に変動しない
- 観点別の満点合計と総合点の計算に矛盾がない
- APIキーなどの認証情報が拡張機能の配布ファイルに含まれていない
- 対象のCloudflareアカウント、Worker名、公開URL、デプロイversionが作業記録に残っている
- 許可した拡張機能からのみ評価でき、本番でOriginなしのリクエストを許可していない
- Workers AIのNeuron使用量、4xx、5xx、タイムアウト、429を確認できる
- 直前の正常なversionへロールバックする手順を実施できる

## 7. フェーズ3: 採点結果画面

### 作業

1. 100点満点の総合点を表示する
2. 観点別の得点と満点を表示する
3. 良かった点と不足点を分けて表示する
4. ユーザーの回答と模範解答を比較できるように表示する
5. 採点後は同じ回答を再送信できないようにする
6. 新しいトレーニングを開始する操作を用意する
7. APIエラー時には回答を失わず、再送信できるようにする

### 完了条件

- 回答前に模範解答が表示されない
- 採点後に総合点、観点別得点、良かった点、不足点、模範解答がすべて表示される
- 1回限りの回答というルールが画面操作上も守られる
- 長いコードや長い模範解答でもサイドパネルのレイアウトが崩れない

## 8. フェーズ4: 読解サポート

読解サポートはトレーニングと候補選択の仕組みを共有するが、入力、API契約、画面状態、結果を分離する。トレーニングの「回答前に模範解答を見せない」という原則や1回限りの回答状態へ影響させない。

APIのフィールド、上限、タイムアウト、エラー、段階別レスポンスは[読解サポートAPI契約 v1](reading-support-api-contract.md)と `contracts/reading-support/v1/` を正本とする。

### 状態の共有と分離

| 種類             | 状態                                                                           |
| ---------------- | ------------------------------------------------------------------------------ |
| 共通             | 現在のGitHubページ、対象コード、ソースURL、API接続許可、利用回数制限の待機期限 |
| トレーニング専用 | 回答、採点中・完了・エラー、採点結果、模範解答、1回限り制約                    |
| 読解サポート専用 | 質問・調査目的、ガイド生成中・表示・エラー、詳しい説明生成中・表示、完了       |

モード切替では専用状態を相手モードへ表示しない。同じ対象コードの間は各モードの未送信入力を個別に保持し、意図しない入力消失を避ける。ページ遷移または明示的な完了・新規開始では両モードの状態をリセットする。

### 作業

1. 候補選択後にトレーニングと読解サポートを選べるようにする
2. 分からない点または調査目的の入力欄を追加する
3. 空入力、長すぎる入力、コード未選択を検証する
4. 言語、ソースURL、対象コード、質問、支援段階を含むリクエスト契約を定義する
5. 着眼点、確認すべき前提、確認質問、次に確認する候補を固定JSONで返す契約を定義する
6. 対象コードから判断できる範囲を越えて事実を断定しない生成処理を実装する
7. 最初は着眼点と段階的なヒントを表示する
8. ユーザー操作後にだけ詳しい説明を取得して表示する
9. ローディング、APIエラー、再試行、モード変更時の状態を扱う
10. コード本文と質問本文を保存せずに利用状況を計測する

### 支援の段階

| 段階             | 表示する内容                                                   |
| ---------------- | -------------------------------------------------------------- |
| 読むためのガイド | 注目すべき処理や識別子、確認すべき前提、理解を進めるための質問 |
| 詳しい説明       | ユーザーが明示的に求めた場合の、対象コードに限定した説明       |

MVPでは、別ファイルの内容、実際の呼び出し元、リポジトリ全体の設計意図を取得しない。対象コードだけでは確認できない内容は、不明であることと次に確認すべき候補を示す。

### 完了条件

- トレーニングと読解サポートを選択できる
- 質問または調査目的を送信し、着眼点、確認質問、ヒントを確認できる
- ユーザーが希望した場合だけ詳しい説明を取得できる
- トレーニングと読解サポートの入力・結果が混在しない
- エラー時に入力を失わず再試行できる
- 対象コードから判断できない内容を確定事項として表示しない
- コード本文と質問本文をログや計測イベントへ保存しない

## 9. フェーズ5: おすすめ候補の抽出

Pythonファイル本文の取得、関数・メソッドの構文範囲、対応構文、実装とテストの境界は、[Python関数・メソッド候補の抽出方式](python-candidate-extraction.md)を正本とする。

### 作業

1. 現在のPythonファイル本文を取得する
2. `def`、`async def`、クラス内メソッドの範囲を抽出する
3. デコレータを対象コードに含める
4. 短すぎる関数、単純なアクセサ、自動生成らしいコードを除外する
5. 残った候補をウォームアップ、おすすめ、チャレンジに分類する
6. 難易度、推定時間、推薦理由を表示する
7. 選ばれた候補をトレーニングと同じ回答・採点フローへ渡す

### 実装方針を決める際の比較対象

- ブラウザで利用できるPythonパーサーを拡張機能へ同梱する
- 評価API側でPython ASTを使って抽出する
- 初期版のみ、限定的なルールで候補を抽出する

正規表現だけで完全なPython構文を扱うことは避ける。複数行シグネチャ、デコレータ、ネストした関数、非同期関数を考慮して方式を選ぶ。

比較の結果、拡張機能へ `@lezer/python` を同梱してBackground Worker内で構文解析し、GitHub Contents REST APIから取得した本文を構文ノードの範囲で切り出す方式を採用する。採用理由と不採用方式の詳細は抽出方式の文書を参照する。

### 完了条件

- 一般的なPythonファイルから最大3件の候補を提示できる
- 候補として表示したコード範囲が途中で切れていない
- 候補が見つからない場合、別のPythonファイルで再試行できると案内する
- 候補選択後はトレーニングと同じ採点結果まで到達できる

## 10. フェーズ6: 品質確認とリリース準備

### 機能確認

- GitHub以外のページ
- GitHubのPythonファイル
- Python以外のファイル
- 候補が見つからないファイル
- 短い候補コードと長い候補コード
- 日本語の回答と英語の回答
- 日本語の質問と英語の質問
- 正しい回答、不完全な回答、誤った回答
- 読解サポートのガイド表示と詳しい説明の追加表示
- トレーニングと読解サポートを切り替えた場合の状態
- APIタイムアウト、回数制限、サーバーエラー
- GitHub内でのページ遷移後の動作

### セキュリティとプライバシー

- APIキーや秘密情報が拡張機能に含まれていないことを確認する
- public repository以外を対象外にする
- AIへ送信するデータを送信前にユーザーへ示す
- コード、回答、質問をログや計測イベントへ保存しない
- `innerHTML` を使わず、外部入力をテキストとして表示する
- APIに入力サイズ制限と利用回数制限を設ける

### 計測

最低限、次のイベントを区別して計測できるようにする。

- サイドパネルを開いた
- おすすめ候補を選んだ
- トレーニングまたは読解サポートを選んだ
- 回答を提出した
- 採点結果と模範解答を表示した
- トレーニングを完了した
- 読解サポートの質問を提出した
- 着眼点とヒントを表示した
- 詳しい説明を表示した
- エラーが発生した

コード本文、回答本文、質問本文は、分析イベントへ含めない。

### 完了条件

- MVP必須機能を実際のGitHubページ上で一通り操作できる
- 正常系と主要な異常系の確認項目が記録されている
- Chrome拡張を新規に読み込んだ環境で再現できる
- 収集する情報と外部AIへの送信についてユーザーが確認できる

## 11. テスト方針

### 単体テスト

- GitHub URLとPythonファイルの判定
- 入力文字数と必須入力の検証
- 評価APIレスポンスの形式検証
- 読解サポートAPIのリクエストとレスポンスの形式検証
- 観点別配点と総合点の計算
- Pythonの関数・メソッド候補抽出
- モード選択、支援段階、リセットの状態遷移

### 結合テスト

- Content ScriptからSide Panelへのページコンテキスト受け渡し
- Background WorkerからSide Panelへの候補コード受け渡し
- Side PanelからBackground Workerを経由した評価API呼び出し
- Side PanelからBackground Workerを経由した読解サポートAPI呼び出し
- APIレスポンスから採点結果画面への反映
- 読解サポートのガイド表示から詳しい説明表示までの遷移
- エラー発生時の回答保持と再送信
- エラー発生時の質問保持と再送信

### 手動確認

- GitHubの画面構造が異なる複数のrepositoryで確認する
- サイドパネルの狭い幅で表示を確認する
- GitHub内のクライアントサイド遷移後にも動作することを確認する

## 12. 実装単位のチェックリスト

- [x] 評価軸の配点とAPI契約を確定する
- [x] Pythonファイル判定を実装する
- [x] トレーニングの入力検証を実装する
- [x] 回答送信と画面状態管理を実装する
- [x] 評価APIを実装する
- [x] AI出力の形式検証を実装する
- [x] Cloudflare Workers AIを設定して評価APIをデプロイする
- [x] 採点結果画面を実装する
- [x] 模範解答の回答後表示を実装する
- [x] 新しいトレーニングの開始操作を実装する
- [x] 読解サポートAPIの契約を確定する
- [x] 読解サポートの生成処理を実装する
- [x] モード選択と質問入力を実装する
- [x] 着眼点、ヒント、詳しい説明の表示を実装する
- [x] Pythonの候補抽出方式を決定する
- [x] おすすめ候補の抽出と表示を実装する
- [x] 読解サポートの正常系と主要な異常系をテストする
- [x] 読解サポートのプライバシー表示と利用制限を確認する
- [x] MVPの読解サポート計測イベントを実装する

## 13. MVP後に検討すること

- 回答の書き直しと再評価
- 学習履歴と過去回答の比較
- 評価の納得感に対するユーザーフィードバック
- 採点基準の調整と評価の安定性改善
- 対応言語の追加
- VS Code拡張とのデータモデル共通化
- 複数ファイルを横断した定義、参照、呼び出し元の追跡
- 読んだファイルやシンボル、調査経路の記録
